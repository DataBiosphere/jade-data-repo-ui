language: node_js
node_js:
  - 8
  - 10
  - node


services:
  - docker



deploy:
  provider: script
  script: echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin && docker_push
  on:
    branch: develop
